# 光纤端面微纳结构传感标定数据分析

## 说明

本协议面向光纤端面微纳结构器件的传感功能验证，用于演示 Airalogy Protocol 如何把器件信息、标定数据上传、自动拟合、质控判定、绘图和报告生成串联起来。

示例场景：学生在光纤端面加工纳米孔阵列或超表面结构后，采集不同折射率样品下的反射谱共振峰位置。上传 CSV 后，协议自动计算灵敏度、线性拟合优度、检测限估计和标定曲线。

## 器件与实验信息

记录人：{{var|record_recorder: UserName}}
记录时间：{{var|record_time: CurrentTime}}
实验编号：{{var|experiment_id: str = "FIBER-SENS-XXXXXXXX"}}
器件名称：{{var|device_name: str = "光纤端面纳米孔阵列折射率传感器"}}
结构类型：{{var|structure_type: str = "Au 纳米孔阵列 / 端面超表面"}}
光纤类型：{{var|fiber_type: str = "SMF-28 单模光纤"}}
传感对象：{{var|measurand: str = "液体折射率"}}
自变量单位：{{var|stimulus_unit: str = "RIU"}}
读出信号：{{var|readout_signal: str = "反射谱共振峰中心波长"}}
测试温度（℃）：{{var|test_temperature_c: float = 25.0}}
基线波长噪声（pm）：{{var|baseline_noise_pm: float = 5.0, ge = 0}}

## 数据上传

请上传传感标定数据（CSV 格式）：

{{var|calibration_file: FileIdCSV}}

<details>
<summary>CSV 格式要求和示例</summary>

必须包含以下列：

| stimulus | resonance_nm | intensity_db |
|---------:|-------------:|-------------:|
| 1.3330   | 1548.20      | -24.8        |
| 1.3380   | 1549.78      | -24.6        |
| 1.3430   | 1551.34      | -24.5        |

字段说明：

- `stimulus`：标定自变量，例如折射率、温度、压力或浓度。
- `resonance_nm`：对应条件下拟合得到的共振峰中心波长，单位 nm。
- `intensity_db`：共振谷或峰强度，可选；本示例主要用于保留原始光谱读数摘要。

</details>

## 自动计算结果

标定点数量：{{var|point_count: int}}
自变量最小值：{{var|stimulus_min: float}}
自变量最大值：{{var|stimulus_max: float}}
共振峰漂移范围（nm）：{{var|wavelength_shift_span_nm: float}}
灵敏度（nm / 自变量单位）：{{var|sensitivity_nm_per_unit: float}}
拟合截距（nm）：{{var|intercept_nm: float}}
线性拟合优度 R<sup>2</sup>：{{var|r2: float}}
估计检测限（自变量单位）：{{var|lod_unit: float}}
响应单调性通过：{{var|monotonic_pass: bool}}
质控通过：{{var|qc_pass: bool}}

<details>
<summary>质控标准</summary>

满足以下条件时，判定为 QC Pass：

- 至少包含 5 个有效标定点；
- 自变量范围不为 0；
- 共振峰随自变量单调变化；
- 线性拟合优度 R<sup>2</sup> 不低于 0.98；
- 灵敏度绝对值大于 0，可据此估计检测限。

</details>

## 自动绘图

标定曲线：
{{var|chart_calibration: FileIdSVG}}

## 自动生成报告

{{var|report: AiralogyMarkdown}}
