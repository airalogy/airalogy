---
"@airalogy/aimd-renderer": patch
"@airalogy/aimd-recorder": patch
---

Add default prose typography for rendered AIMD documents inside an `.aimd-renderer` container, including clearer heading hierarchy, visible list markers, and more compact body line height. Recorder rendered content and markdown previews now inherit those renderer prose styles instead of keeping a separate heading/list treatment.
