---
"@airalogy/aimd-recorder": patch
---

Keep `AimdRecorderEditor` source and recorder panels contained inside the fitted workbench height so long AIMD documents scroll inside the editor instead of stretching the host page.

Also pass quiz grading, submitted state, and quiz explanation display options through the combined editor and recorder-side visual editing surface.
