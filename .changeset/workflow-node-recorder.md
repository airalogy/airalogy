---
"@airalogy/aimd-recorder": patch
---

Add an optional `initialSourceCollapsed` prop to `AimdRecorderEditor` so embedded recorder surfaces can open directly on the record form while still allowing users to expand the source panel.
