---
"@airalogy/aimd-recorder": patch
---

Keep assigner controls visible for structured `list[...]` and object-like var fields after assigners populate them, and render those values as editable JSON text instead of lossy string coercions.
