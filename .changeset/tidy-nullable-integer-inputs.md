---
"@airalogy/aimd-recorder": patch
---

Render nullable integer annotations with the same native number input and stepper controls as non-nullable integers while preserving empty values as `null`, and prevent required markers from being clipped by compact field headers.
