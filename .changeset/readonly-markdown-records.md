---
"@airalogy/aimd-renderer": patch
---

Render readonly `AiralogyMarkdown` record values through the AIMD Vue renderer so completed records display Markdown structure, nested AIMD preview tokens, and resolved Markdown image assets instead of raw Markdown text.
