---
"@airalogy/aimd-renderer": patch
---

Include host-provided Record metadata columns in the shared table column picker and expose a controlled `metadataColumnKeys` model while keeping every metadata column visible by default.
