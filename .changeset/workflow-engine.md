---
"airalogy-engine": patch
---

Add `AiralogyWorkflowEngine` for executing AIMD workflow transition assignments, including workflow-level Python assigners, transition output namespaces, target Record drafts, conditions, and iteration limits.
